# Dependências diretas — versões exatas do ambiente de desenvolvimento (pip freeze)
# Gerado em 2026-05-01. Para atualizar: pip freeze > requirements.txt

# API
# fastapi 0.136.3 é release malicioso (MAL-2026-4750, 2026-05-23): injeta dependência
# oculta 'fastar' no extra [standard]. Sem fix-forward; fixado em 0.136.1 (última limpa).
fastapi==0.136.1
uvicorn==0.51.0
starlette==1.3.1
python-multipart==0.0.32
# Servidor de produção: systemd sobe `gunicorn -w 4 -k uvicorn_worker.UvicornWorker api:app`.
# Pinado aqui para entrar no pip-audit e no Dependabot — antes ficava só instalado à mão
# na VM, invisível para o CI. Não é usado em dev (uvicorn direto).
gunicorn==26.0.0
# O módulo `uvicorn.workers` foi descontinuado (DeprecationWarning desde a 0.51.0) e será
# removido; quando isso acontecer, o restart do scpi-api derruba a API. uvicorn-worker é o
# sucessor oficial, mesmo mantenedor, código idêntico ao módulo antigo.
uvicorn-worker==0.4.0

# Config
python-dotenv==1.2.2

# Banco de dados
psycopg2-binary==2.9.12
pg8000==1.31.5
scramp==1.4.15

# Schemas / validação
pydantic==2.13.4
pydantic_core==2.46.4
email-validator==2.3.0

# Autenticação
# PyJWT sem o extra [crypto]: `cryptography` só serve aos algoritmos ASSIMÉTRICOS
# (RS*/ES*/PS*), e o SCPI assina em HS256, que o PyJWT resolve com hmac/hashlib da
# stdlib — `jwt/utils.py` embrulha o import dela em try/except ModuleNotFoundError.
# Removida em 2026-08-05 com a suíte inteira (700 testes) rodando com o import
# bloqueado, junto de cffi e pycparser, que só existiam como transitivas dela.
# Motivo: era pura superfície de advisory (já custou a PR #107, CVE-2026-69247).
# Se `core.auth_utils.ALGORITHM` deixar de ser HS*, ela precisa voltar —
# test_jwt_algoritmo_simetrico.py cobra isso.
PyJWT==2.13.0
passlib==1.7.4

# AWS
boto3==1.43.56
botocore==1.43.56
s3transfer==0.19.2

# Reconhecimento facial (necessário apenas na máquina com câmera)
# Em servidor headless, pode ser removido se o script não rodar lá
opencv-python==4.13.0.92
numpy==2.5.1

# Rate limiting
slowapi==0.1.10
limits==5.8.0

# Email
resend==2.34.0

# Geração de PDF (export LGPD Art. 18)
reportlab==5.0.0

# Observabilidade — telemetria de erros (Sentry SaaS free tier).
# DSN vem de SENTRY_DSN; ausente = no-op, dev não precisa.
sentry-sdk==2.66.1

# Dependências transitivas (pinadas para reproducibilidade)
annotated-types==0.8.0
anyio==4.14.2
certifi==2026.7.22
charset-normalizer==3.4.9
click==8.4.2
colorama==0.4.6
Deprecated==1.3.1
dnspython==2.8.0
h11==0.16.0
idna==3.18
jmespath==1.1.0
packaging==26.2
python-dateutil==2.9.0.post0
requests==2.34.2
six==1.17.0
typing-inspection==0.4.2
typing_extensions==4.16.0
urllib3==2.7.0
wrapt==2.2.2
