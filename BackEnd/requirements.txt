# Dependências diretas — versões exatas do ambiente de desenvolvimento (pip freeze)
# Gerado em 2026-05-01. Para atualizar: pip freeze > requirements.txt

# API
# fastapi 0.136.3 é release malicioso (MAL-2026-4750, 2026-05-23): injeta dependência
# oculta 'fastar' no extra [standard]. Sem fix-forward; fixado em 0.136.1 (última limpa).
fastapi==0.136.1
uvicorn==0.51.0
starlette==1.3.1
python-multipart==0.0.32
# Servidor de produção: systemd sobe `gunicorn -w 4 -k uvicorn.workers.UvicornWorker api:app`.
# Pinado aqui para entrar no pip-audit e no Dependabot — antes ficava só instalado à mão
# na VM, invisível para o CI. Não é usado em dev (uvicorn direto).
gunicorn==25.3.0

# Config
python-dotenv==1.2.2

# Banco de dados
psycopg2-binary==2.9.12
pg8000==1.31.5
scramp==1.4.12

# Schemas / validação
pydantic==2.13.4
pydantic_core==2.46.4
email-validator==2.3.0

# Autenticação
PyJWT==2.13.0
passlib==1.7.4
cryptography==49.0.0

# AWS
boto3==1.43.51
botocore==1.43.51
s3transfer==0.19.1

# Reconhecimento facial (necessário apenas na máquina com câmera)
# Em servidor headless, pode ser removido se o script não rodar lá
opencv-python==4.13.0.92
numpy==2.5.1

# Rate limiting
slowapi==0.1.10
limits==5.8.0

# Email
resend==2.34.0

# Geração de PDF (export LGPD Art. 18)
reportlab==5.0.0

# Dependências transitivas (pinadas para reproducibilidade)
annotated-types==0.7.0
anyio==4.14.2
certifi==2026.6.17
cffi==2.1.0
charset-normalizer==3.4.9
click==8.4.2
colorama==0.4.6
Deprecated==1.3.1
dnspython==2.8.0
h11==0.16.0
idna==3.18
jmespath==1.1.0
packaging==26.2
pycparser==3.0
python-dateutil==2.9.0.post0
requests==2.34.2
six==1.17.0
typing-inspection==0.4.2
typing_extensions==4.16.0
urllib3==2.7.0
wrapt==2.2.2
