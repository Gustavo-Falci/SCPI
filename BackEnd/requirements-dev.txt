# Dependências de DESENVOLVIMENTO/TESTE — propositalmente fora do requirements.txt
# (produção não instala ferramentas de teste).
# Setup local completo:  pip install -r requirements.txt -r requirements-dev.txt
# Windows: instalar também tzdata (ZoneInfo não acha o banco de fusos do SO):
#   pip install tzdata
pytest==9.0.3
httpx==0.28.1
